build:
  docker:
    web: Dockerfile
  config:
    FLASK_ENV: production

run:
  web: gunicorn 'app:create_app()' --workers 4 --bind 0.0.0.0:$PORT --log-level INFO

release:
  image: web
  command:
    - flask db upgrade
